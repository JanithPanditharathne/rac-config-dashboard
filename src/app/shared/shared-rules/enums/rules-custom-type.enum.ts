/**
 * Enum to represent rule custom types.
 * @enum RulesCustomType
 */
export enum RulesCustomType {
  CHANNELS = 'channels',
  PAGES = 'pages',
  PLACEHOLDERS = 'placeholders',
  BRANDS = 'brands',
  CATEGORY = 'category',
  DEPARTMENT = 'department',
  REVIEW_COUNT = 'reviewCount',
  RATING = 'rating',
}
