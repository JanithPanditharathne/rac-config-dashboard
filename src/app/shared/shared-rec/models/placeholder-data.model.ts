import { DropDownDataItem } from '../../shared-common/models';

/**
 * Interface representing placeholder data model.
 * @interface PlaceholderData.
 */
export interface PlaceholderData {
  placeholders: DropDownDataItem[];
}
