import { DropDownDataItem } from '../../shared-common/models';

/**
 * Interface representing metadata model.
 * @interface MetaData.
 */
export interface MetaData {
  metadata: DropDownDataItem[];
}
