// components
import { AppMainComponent, BaseComponent, LogoutConfirmPopupComponent } from './components';

/**
 * Export all main components.
 */
export const COMPONENTS = [AppMainComponent, LogoutConfirmPopupComponent, BaseComponent];

/**
 * Export all main entry components.
 */
export const ENTRY_COMPONENTS = [LogoutConfirmPopupComponent];
