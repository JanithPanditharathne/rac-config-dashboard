import { Recommendation } from './recommendation.model';

/**
 * Interface representing display rec model.
 * @interface DisplayRecommendation.
 */
export interface DisplayRecommendation {
  recs: Recommendation[];
}
